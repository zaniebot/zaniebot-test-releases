# zaniebot-test-releases

Playground repository for reproducing GitHub tag and release rule behavior.

Use the `Release Lab` workflow to run experiments against a `release` environment protected by a tag ruleset.

Second commit for SHA-mismatch experiments.
